import FormField from "./FormField";

const ImageUploader = ({ name }: { name: string }) => {
  return (
    <FormField name={name}>
      <div></div>
    </FormField>
  );
};

export default ImageUploader;
